let umidade = 10;


if (umidade > 70) {
    console.log ("A umidade do ar está boa.");
} else if((umidade) >= 30 && (umidade) < 70 ) {
    console.log ("O ar está seco.");
} else if (umidade < 30) {
    console.log ("O ar está muito seco");
}